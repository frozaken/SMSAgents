{
  "name": "smsagents",
  "version": "0.1.0",
  "description": "Local topic-based messaging for AI agent sessions",
  "type": "module",
  "license": "MIT",
  "repository": "https://github.com/frozaken/SMSAgents.git",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist-test/*.test.js",
    "validate:plugin": "python3 /Users/marcusteller/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.17.0",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  }
}
